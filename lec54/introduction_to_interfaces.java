public class introduction_to_interfaces {
    public static void main(String[] args) {
        // interface is a bridge between two things like buttons is a interface between TV and Humans
        // only interface has definition of methods
        // When from abstract class concreate class is created the "extends" keyword is used
        // and in case of interfaces, in time of class creation the "implements" keyword is used
        // interface object can not be created but reference can be created
        // interface have to have all abstract methods but in abstract class it is not necessary
        // you can not create a class using multiple abstract class (at the same time) but you can create a class using multiple interface (at the same time)
    }
}

